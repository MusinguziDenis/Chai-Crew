class User {
  final String uid;

  User({this.uid});
  //defines the properties that we want to return
}

class UserData {
  final String uid;
  final String name;
  final String sugars;
  final int strength;

  UserData({this.uid, this.sugars, this.strength, this.name});
}
